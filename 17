'''В файле содержится последовательность из 10 000 целых положительных чисел. Каждое число не превышает 10 000. Определите и запишите в ответе сначала количество пар элементов последовательности, разность которых четна и хотя бы одно из чисел делится на 31, затем максимальную из сумм элементов таких пар. В данной задаче под парой подразумевается два различных элемента последовательности. Порядок элементов в паре не важен.'''

f = open('fw/17.txt').readlines()
s = list(int(i) for i in f)
c = 0
maxi = []
for l in range(0, len(s)):
  for j in range(l + 1, len(s)):
    if ((s[l] - s[j]) % 2 == 0) and ((s[l] % 31 == 0) or (s[j] % 31 == 0)):
      c += 1
      maxi.append(s[l] + s[j])

print(c, max(maxi))

'''В файле содержится последовательность из 10 000 целых положительных чисел. Каждое число не превышает 10 000. Определите и запишите в ответе сначала количество пар элементов последовательности, для которых произведение элементов делится без остатка на 10, затем максимальную из сумм элементов таких пар. В данной задаче под парой подразумевается два различных элемента последовательности. Порядок элементов в паре не важен.'''

f = open('fw/171.txt').readlines()
s = list(int(i) for i in f)
c = 0
maxi = []
for l in range(0, len(s)):
  for j in range(l + 1, len(s)):
    if (s[l] * s[j]) % 10 == 0:
      c += 1
      maxi.append(s[l] + s[j])

print(c, max(maxi))


"""Файл содержит последовательность неотрицательных целых чисел, не превышающих 10 000. Назовём парой два идущих подряд элемента последовательности. Определите количество пар чисел, произведение которых кратно 15, а их сумма делится на 7. В ответе запишите два числа: сначала количество найденных пар, а затем  — максимальную сумму элементов таких пар."""

f = open('fw/173.txt').readlines()
s = list(int(i) for i in f)
c = 0
maxi = []
for l in range(0, len(s) - 1):
    if ((s[l] * s[l + 1]) % 15 == 0) and ((s[l] + s[l + 1]) % 7 == 0):
      c += 1
      maxi.append(s[l] + s[l+1])

print(c, max(maxi))

'''В файле содержится последовательность из 10 000 целых положительных чисел. Каждое число не превышает 10 000. Определите и запишите в ответе сначала количество пар элементов последовательности, для которых произведение элементов делится без остатка на 62, затем максимальную из сумм элементов таких пар. В данной задаче под парой подразумевается два различных элемента последовательности. Порядок элементов в паре не важен.'''
f = open('fw/174.txt').readlines()
s = list(int(i) for i in f)
c = 0
maxi = []
for j in range(0, len(s) - 1):
  for l in range(j + 1, len(s) - 1):
    if (s[j] * s[l]) % 62 == 0:
      c += 1
      maxi.append(s[j] + s[l])

print(c, max(maxi))

# В файле содержится последовательность натуральных чисел.
# Её элементы могут принимать целые значения от 1 до 100 000
# включительно. Определите количество пар последовательности, в которых
# остаток от деления хотя бы одного из элементов на 16 равен минимальному
# элементу последовательности. В ответе запишите количество найденных
# пар, затем максимальную из сумм элементов таких пар. В данной задаче под
# парой подразумевается два идущих подряд элемента последовательности.

f = open('fw/171.txt').readlines()
s = [int(i) for i in f]
c = 0
maxi = []
for j in range(0, len(s) - 1):
    if ((s[j] % 16 == min(s)) or (s[j+1] % 16 == min(s))):
        c+=1
        maxi.append(s[j] + s[j+1])

print(c, max(maxi))

f = open('fw/17.txt').readlines()
s = list(int(i) for i in f)

c = 0 
mas = []
mas11 = [int(s[j]) for j in range(len(s)) if s[j] % 11 == 0]
for l in range(0, len(s) - 2):

    if (((s[l] % 100 == 11) + (s[l + 1] % 100 == 11) + (s[l + 2] % 100 == 11)) >= 1)\
    and (((s[l] + s[l+1] + s[l+2]) / 3) > (sum(mas11) / len(mas11))):
        c += 1
        mas.append(s[l] + s[l+1] + s[l+2])
print(c, max(mas))



# В файле содержится последовательность целых чисел, каждое из которых находится в диапазоне от 
# -20 000 до 20000. Определите количество пар элементов последовательности, сумма которых превосходит 
# каждую из сумм соседних пар, при этом все три суммы являются положительными.

# В ответе запишите количество найденных пар чисел, затем минимальное из произведений элементов таких пар.

# В данной задаче под парой подразумевается два идущих подряд элемента последовательности. 
# Под соседними парами подразумеваются четыре идущих подряд элемента последовательности.
# Например, в последовательности 1 2 3 4 5 6 у пары (3, 4) соседние пары (1, 2) и (5, 6)

f = open('fw/1710.txt').readlines()
s = list(int(i) for i in f)
c = 0
minn = []
for j in range(0, len(s)-4):
    if s[j - 1] + s[j] < s[j + 1] + s[j + 2] > s[j + 3] + s[j + 4]:
        if (s[j - 1] + s[j]) > 0 and (s[j + 1] + s[j + 2]) > 0 and (s[j + 3] + s[j + 4]) > 0:
            minn.append(s[j - 1] * s[j])
            c += 1

print(c, min(minn))


def d(j, l):
    p = []
    lo = []
    lq = []
    for n in range(1, 1001):
        if j % n == 0:
            lo.append(n)
    for n in range(1, 1001):
        if l % n == 0:
            lq.append(n)

    for i in range(0, len(lo)):
        for j in range(0, len(lq)):
            if lo[i] == lq[j]:
                p.append(lo[i])
    return p

f = open('171.txt').readlines()
s = list(int(i) for i in f)
c = 0
summ = []
for j in range(0, len(s)  - 1):
    if len(d(s[j], s[j+1])) == 1 and (s[j] % 2 != s[j+1] % 2) :
        summ.append(s[j] + s[j+1])
        c+=1

print(c, min(summ))



from math import gcd
f = [int(x) for x in open('171.txt')]
k = 0
m = []
for i in range(len(f) - 1):
    if gcd(f[i], f[i+1]) == 1 and (f[i] % 2 != f[i + 1] % 2):
        k += 1
        m.append(f[i] + f[i + 1])
print(k, min(m))


# В файле содержится последовательность натуральных чисел. Её элементы могут принимать целые значения от 1 до 100 000 включительно.
#  Определите количество троек последовательности, в которых хотя бы один элемент кратен наименьшему 
# числу последовательности, кратному 2025, а сумма тройки при этом — шестизначное число.

# В ответе запишите количество найденных троек, затем максимальную из сумм элементов таких троек.

# В этой задаче под тройкой подразумеваются три идущих подряд элемента последовательности.

f = open('171.txt').readlines()
s = [int(i) for i in f]
maxi = []
qwe = []
for l in range(0, len(s)):
    if s[l] % 2025 == 0:
        qwe.append(s[l])
qwe25 = min(qwe)

for j in range(0, len(s) - 2):
    if (s[j] % qwe25 == 0 or s[j + 1] % qwe25 == 0 or s[j + 2] % qwe25 == 0) and (100000 <= (s[j] + s[j + 1] + s[j + 2]) <= 999999):
        maxi.append(s[j] + s[j + 1] + s[j + 2])

print(len(maxi), max(maxi))



# В файле содержится последовательность натуральных чисел. Элементы последовательности могут принимать целые значения 
# от 1 до 10 000 включительно. Определите количество пар последовательности, сумма элементов которой больше
#  максимального кратного 17-ти числа в последовательности.

# В ответе запишите количество найденных пар, затем максимальную из сумм элементов таких пар.
#  В данной задаче под парой подразумевается два идущих подряд элемента последовательности.



f = open('172.txt').readlines()
s = [int(i) for i in f]
c = 0
maxi = [] 
qwe = []
for l in range(0, len(s)):
    if s[l] % 17 == 0:
        qwe.append(s[l])

maxx = max(qwe)
for j in range(0, len(s) - 1):
    if (s[j] + s[j + 1]) > maxx:
        maxi.append(s[j] + s[j+1])
    
print(len(maxi), max(maxi))


